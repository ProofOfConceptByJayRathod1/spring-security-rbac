server:
  port: 9095
spring:
  datasource:
    username: sa
    password: password
    url: jdbc:mysql://mysql-standalone:3306/spring-security-rbac?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  #JPA Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect